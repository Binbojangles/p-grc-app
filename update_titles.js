const fs = require("fs"); const jsonData = JSON.parse(fs.readFileSync("cmmc_level2_controls.json", "utf8")); const cuiDomains = ["AC.L2", "IA.L2", "MP.L2", "PE.L2", "SC.L2", "SI.L2"]; jsonData.controls.forEach(control => { if (cuiDomains.includes(control.domain_id) && !control.title.includes("[CUI Data]")) { control.title = `${control.title} [CUI Data]`; } }); fs.writeFileSync("cmmc_level2_controls.json", JSON.stringify(jsonData, null, 2)); console.log("Updated control titles in cmmc_level2_controls.json");
